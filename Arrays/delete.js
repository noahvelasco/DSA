const arr = [1, 2, 3, 10, 11, 0];
console.log(arr);
let x = 22;

let a = arr.pop(); //removes element at end
console.log("pop() removed at end ", a, arr);
let b = arr.shift();
console.log("shift() removed at front ", b, arr); //removes element at beginning
